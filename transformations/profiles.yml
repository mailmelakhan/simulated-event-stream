transformations:
  target: dev
  outputs:
    dev:
      type: bigquery
      threads: 16
      database: "{{ var('PROJECT_ID') }}"
      schema: "{{ var('BQ_DATASET') }}"
      method: "{{ var('AUTH_METHOD') }}"
      location: US
      dataproc_batch: null